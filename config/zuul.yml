server:
  port: 8000
spring:
  application:
    name: zuul
##
zuul:
  routes:
    api-a:
      path: /menu/**
      serverId: menu
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
