server:
  port: 8000
spring:
  application:
    name: zuul
##
zuul:
  routes:
    api-a:
      path: /order/**
      serverId: order
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
